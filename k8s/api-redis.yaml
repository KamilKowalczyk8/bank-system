apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-redis-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-redis
  template:
    metadata:
      labels:
        app: api-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: api-redis
spec:
  selector:
    app: api-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379