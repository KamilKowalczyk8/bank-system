spring:
  application:
    name: audit-service

  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: audit-group
      auto-offset-reset: earliest
      value-serializer: org.apache.kafka.common.serialization.StringSerializer      properties:
        spring.json.trusted.packages: "com.bank.common.dto"



