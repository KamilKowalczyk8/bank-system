spring:
  datasource:
    url: jdbc:postgresql://auth-postgres:5432/authdb
    username: authuser
    password: authpass

  kafka:
    bootstrap-servers: bank-kafka:29092

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false