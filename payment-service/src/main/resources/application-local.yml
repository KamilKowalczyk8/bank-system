spring:
  datasource:
    url: jdbc:postgresql://localhost:5459/paymentdb
    username: paymentuser
    password: paymentpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092

services:
  account:
    url: http://localhost:8084
  fraud:
    url: http://localhost:8089